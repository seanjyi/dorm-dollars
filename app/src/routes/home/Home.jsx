import React, { Component } from 'react'
import Navbar from '../../components/Navbar';

const Home = () => {

    // if (!props.loggedIn) {
    //     return <Navigate to="/login" />
    // }

    return (
        <p>Hello World</p>
    )
}

export default Home;