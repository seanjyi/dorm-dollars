import React, { Component } from 'react'
import Navbar from '../../components/Navbar';

const Home = () => {

    // if (!props.loggedIn) {
    //     return <Navigate to="/login" />
    // }

    return (
        <>
            <Navbar />
        </>
    )
}

export default Home;